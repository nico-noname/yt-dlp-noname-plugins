[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "yt-dlp-noname-plugins"
version = "1.0.0"
description = "noname's yt-dlp plugin"
dependencies = [
    "yt-dlp",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["yt_dlp_plugins*"]